import leaveAC from './actionCreators'

const sendRequest = () => async (dispatch, getState) => {
    dispatch(leaveAC.pending)
    try{
        const request = {
            
        }
    } catch (e) {

    }
}

export default {

}
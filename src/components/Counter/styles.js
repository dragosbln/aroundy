import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    base:{
        height: '100%',
        justifyContent: 'space-between',
        alignItems: 'center',
      },
      counterHeading: {
        marginBottom: 3
      },
      counterFooter: {
        color: '#C2C2C3'
      },
})
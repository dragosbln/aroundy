import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    base: {
        width: '80%',
        height: '100%',
        justifyContent: 'center',
        alignItems: 'center',
    },
    icon: {
        width: 28,
        height: 28,
    }
})